@page "/faculty/takeattendance"
<!doctype html>
  <head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>take attendance</title>
  </head>

<body>

 <h2>take attendance for {{today}}</h2> 
      <form method = "Post" action = "{{ url_for('facultytakeattendance') }}">
        {{ form.csrf_token }}
        {{ form.attended.label }}
        {{ form.attended }}
        <br>
        {{ form.classid.label }}
        {{ form.classid }}
        <br>
        {{ form.slotid.label }}
        {{ form.slotid }}

        

      <input type = "submit" id = "form" name = "form" value = "submit" >
    </form>

  
</body>