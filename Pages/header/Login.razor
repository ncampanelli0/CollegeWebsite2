@page "/header/login"

<!doctype html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>login</title>
</head>
  <body>



  <form method = "Post" action = "{{ url_for('login') }}" class="flex flex-col pt-3 md:pt-8">
      {{ form.csrf_token }}
      {{ form.username.label }}
      {{ form.username }}
      {{ form.password.label }}
      {{ form.password }}
  
      <input type = "submit" value = "submit" class>
    </form>

  </body>